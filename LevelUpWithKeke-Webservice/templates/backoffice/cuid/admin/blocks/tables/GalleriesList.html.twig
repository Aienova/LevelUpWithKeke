    <div id="list-galleries" >
    <div class="flexible">

        {% set i = 0 %}
        {% for gallery in galleries %}


        <div id="gallery-{{gallery.id}}" class="image-card">

            <div class="image" style="background-image:url({{gallery.image}})">
            <h2>{{gallery.title}}</h2>

            
            </div>

            <div class="info">
                <h2>gallery auto #{{gallery.id}} :</h2>
                <ul>                   
                    
                    {#
                    <li>

                        {% if gallery.activated == 1 %}
                        Activé
                        {% else %}
                        Desactivé
                        {% endif %}
                    </li>
                    #}


                    <li>
                    
                    <a href="./edit-gallery/{{gallery.id}}"><button>Modifier cette galerie</button></a>
                    <br>
                    <a target="_blank" href="{{gallery.urlDemo}}"><button>Voir le résultat</button></a>
                    
                    </li>
                </ul>
            
            </div>
        </div>

            {% set i = i + 1 %}

           {% if i is divisible by(3)%} </div><div class="flexible"> {% endif %}

        {% endfor %}
    </div>
    </div>